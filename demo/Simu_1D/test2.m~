clear all;clc;
load system
x0 = [100;0];
u = 100;
tau = 0.05;
y = ode45(@odefun,[0,tau],[x0;u]);
xt = y.y(1:2,end)

Phi = expm(A*tau);
xt_est = Phi*x0 - A\(eye(2)-Phi)*B*u



syms t x1 x2 u
Phi = expm(A*t);

